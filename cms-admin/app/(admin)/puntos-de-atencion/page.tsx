export default function PuntosDeAtencionPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Puntos de atención</h1>
      <p className="mt-2 text-muted-foreground">
        Próximamente — este módulo sigue el mismo patrón CRUD que &quot;Motos&quot; una vez se
        implemente el dominio <code>service-point</code> en el backend.
      </p>
    </div>
  );
}
