export default function NoticiasPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Noticias</h1>
      <p className="mt-2 text-muted-foreground">
        Próximamente — este módulo sigue el mismo patrón CRUD que &quot;Motos&quot; una vez se
        implemente el dominio <code>news</code> en el backend.
      </p>
    </div>
  );
}
