# URL del GraphQL del backend propio (Fase 5 — integración end-to-end).
# Público en el navegador porque solo se usa para queries de catálogo (sin auth).
NEXT_PUBLIC_BACKEND_GRAPHQL_URL=http://localhost:8080/graphql
